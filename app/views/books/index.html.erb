<h1>User info</h1>

<h1>New book</h1>
<!--book投稿フォーム（book index左側）-->
<%= form_with model:@book do |f| %>
  <%= f.label :title, 'Title' %>
  <%= f.text_field :title %>
  
  <%= f.label :body, 'Opinion' %>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>  

